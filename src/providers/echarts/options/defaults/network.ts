let options = {
  xAxis: [{
    type: 'value',
    nameTextStyle: {
      fontSize: 10
    },
    show: false,
    min: 0,
    max: 1000
  }],
  yAxis: [{
    show: false
  }]
  // xAxis: false,
  // yAxis: false,
  // coordinateSystem: 'cartesian2d'
}

export default options
