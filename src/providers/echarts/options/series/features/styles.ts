export default function (props: any) {
  let itemStyle = {}
  let styles = props.styles || {}
  if (styles.colors) {
    if (styles.colors.constructor.name === 'Gradient') {
      
    }
  }
  return {
    itemStyle
  }
}